
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const ViewPlatform: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>ViewPlatform</Text>
        </Container>
    );
};

export default ViewPlatform;
