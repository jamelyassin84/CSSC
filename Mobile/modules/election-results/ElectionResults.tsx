
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const ElectionResults: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>ElectionResults</Text>
        </Container>
    );
};

export default ElectionResults;
